<?php

/**
 * Auto-generated stub file for Moodle's public/backup/util/helper/restore_log_rule.class.php
 *
 * @generated by bimoo — https://github.com/michaelmeneses/bimoo
 * @link https://github.com/michaelmeneses/moodle-stubs
 */
/**
 * Helper class used to restore logs, converting all the information as needed
 *
 * This class allows each restore task to specify which logs (by action) will
 * be handled on restore and which transformations will be performed in order
 * to accomodate them into their new destination
 *
 * TODO: Complete phpdocs
 */
class restore_log_rule implements processable
{
    protected $module;
    // module of the log record
    protected $action;
    // action of the log record
    protected $urlread;
    // url format of the log record in backup file
    protected $inforead;
    // info format of the log record in backup file
    protected $modulewrite;
    // module of the log record to be written (defaults to $module if not specified)
    protected $actionwrite;
    // action of the log record to be written (defaults to $action if not specified)
    protected $urlwrite;
    // url format of the log record to be written (defaults to $urlread if not specified)
    protected $infowrite;
    // info format of the log record to be written (defaults to $inforead if not specified)
    protected $urlreadregexp;
    // Regexps for extracting information from url and info
    protected $inforeadregexp;
    protected $allpairs;
    // to acummulate all tokens and values pairs on each log record restored
    protected $urltokens;
    // tokens present int the $urlread attribute
    protected $infotokens;
    // tokens present in the $inforead attribute
    protected $fixedvalues;
    // Some values that will have precedence over mappings to save tons of DB mappings
    protected $restoreid;
    public function __construct($module, $action, $urlread, $inforead, $modulewrite = null, $actionwrite = null, $urlwrite = null, $infowrite = null)
    {
    }
    public function set_restoreid($restoreid)
    {
    }
    public function set_fixed_values($values)
    {
    }
    public function get_key_name()
    {
    }
    public function process($inputlog)
    {
    }
    // Protected API starts here
    protected function parse_tokens_and_matches($tokens, $values, $content)
    {
    }
    protected function calculate_url_regexp($urlexpression)
    {
    }
    protected function calculate_info_regexp($infoexpression)
    {
    }
    protected function extract_tokens($expression)
    {
    }
    protected function build_regexp($expression, $tokens)
    {
    }
}